<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Login_Signup" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Login_Signup" parent="Base.Theme.Login_Signup" />

    <!-- FIX: Removed invalid MaterialComponent styles. -->
    <!-- These legacy styles are valid and will customize the CalendarView. -->
    <style name="CalenderViewDateCustom" parent="android:TextAppearance.DeviceDefault.Small">
        <item name="android:textColor">@color/oxford_blue</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="CalenderViewWeekCustom" parent="android:TextAppearance.DeviceDefault.Small">
        <item name="android:textColor">#A0AEC0</item>
        <item name="android:textSize">12sp</item>
    </style>

</resources>
